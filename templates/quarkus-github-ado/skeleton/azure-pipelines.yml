trigger:
  branches:
    include:
      - main

# OBRIGATÓRIO: sem este bloco o disparo via API falha com "No pool was specified"
pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: echo "Build da aplicação ${{ '${{ values.appName }}' }}"
    displayName: 'Build'
